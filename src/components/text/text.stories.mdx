import { html } from "lit-html";
import { Story, Preview, Meta, Props } from "@storybook/addon-docs/blocks";
import { action } from "@storybook/addon-actions";
import { withWebComponentsKnobs } from "storybook-addon-web-components-knobs";
import { withKnobs, text, number } from "@storybook/addon-knobs";

import { Text } from "../../../dist/index.js";

<Meta
  title="Components/Text"
  component={Text}
  parameters={{
    component: "sui-text",
    decorators: [withKnobs, withWebComponentsKnobs],
    options: { selectedPanel: "storybookjs/knobs/panel" }
  }}
/>

# &lt;sui-text&gt;

A component used for displaying text. Contains styling for paragraph (`p`) and heading tags (`h1` through `h5`).

## Features:

- Shadow DOM
- Defaults to `<p>`
- Redefine element using `as` prop

<sui-button bg="gray" p="2" href="#">Source code on Github</sui-button>

## Examples

### Paragraph

<Preview withToolbar>
  <Story name="Paragraph">
    {html`
      <sui-text>Lorem ipsum or something right?</sui-text>
    `}
  </Story>
</Preview>

### Heading

<Preview withToolbar>
  <Story name="Heading">
    {html`
      <sui-text as="h1">Lorem ipsum or something right?</sui-text>
    `}
  </Story>
</Preview>

## API

<Props of="sui-text" />
