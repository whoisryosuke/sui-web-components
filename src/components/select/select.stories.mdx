import { html } from "lit-html";
import { Story, Preview, Meta, Props } from "@storybook/addon-docs/blocks";
import { action } from "@storybook/addon-actions";
import { withWebComponentsKnobs } from "storybook-addon-web-components-knobs";
import { withKnobs, text, number } from "@storybook/addon-knobs";

import { Select } from "../../../dist/index.js";

<Meta
  title="Components/Select"
  component={Select}
  parameters={{
    component: "sui-select",
    decorators: [withKnobs, withWebComponentsKnobs],
    options: { selectedPanel: "storybookjs/knobs/panel" }
  }}
/>

# &lt;sui-select&gt;

A component used for linking users between pages. Similar to the native `<select>` element.

## Features:

- Defaults to `<select>` element
- Redefine element using `as` prop

<sui-button bg="gray" p="2" href="#">Source code on Github</sui-button>

## Examples

### Basic

<Preview withToolbar>
  <Story name="Basic">
    {html`
      <sui-select>
        <option value="first">First option</option>
      </sui-select>
    `}
  </Story>
</Preview>

## API

<Props of="sui-select" />
